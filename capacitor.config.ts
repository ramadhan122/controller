import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ramadhan.zzzcontroller',
  appName: 'ZZZ Controller',
  webDir: 'dist'
};

export default config;
